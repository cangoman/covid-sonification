import React from 'react';
import { Button } from 'react-bootstrap';

function RegisterButton() {
	return (
		<Button href='/register' variant='outline-light' className='ml-3'>
			Register
		</Button>
	);
}

export default RegisterButton;
